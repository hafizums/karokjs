import type { KaraokeTranscript } from "@/lib/karaoke/types";
import demoTranscriptJson from "./demo-transcript.json";

/** Prepared Phase 1 demo song (local static assets only). */
export const demoTranscript: KaraokeTranscript = demoTranscriptJson;
